import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function IntegrationPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Integração de Dados</CardTitle>
                <CardDescription>Sincronize dados com seu sistema ERP.</CardDescription>
            </CardHeader>
            <CardContent>
                <p className="text-muted-foreground">Conteúdo da página de integração de dados em breve.</p>
            </CardContent>
        </Card>
    );
}
