import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function GeradorSaidaPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Gerador Saída</CardTitle>
                <CardDescription>Ferramenta para gerar saídas de dados e integrações.</CardDescription>
            </CardHeader>
            <CardContent>
                <p className="text-muted-foreground">Conteúdo da página do Gerador de Saída em breve.</p>
            </CardContent>
        </Card>
    );
}
