import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function CompartilhamentoPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Compartilhamento</CardTitle>
                <CardDescription>Compartilhe relatórios e informações.</CardDescription>
            </CardHeader>
            <CardContent>
                <p className="text-muted-foreground">Conteúdo da página de Compartilhamento em breve.</p>
            </CardContent>
        </Card>
    );
}
