import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function RmConectorPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>RM Conector</CardTitle>
                <CardDescription>Gerencie a conexão com o RM.</CardDescription>
            </CardHeader>
            <CardContent>
                <p className="text-muted-foreground">Conteúdo da página RM Conector em breve.</p>
            </CardContent>
        </Card>
    );
}
