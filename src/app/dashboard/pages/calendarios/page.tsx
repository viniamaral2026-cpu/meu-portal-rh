import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function CalendariosPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Calendários</CardTitle>
                <CardDescription>Visualize calendários de feriados e eventos.</CardDescription>
            </CardHeader>
            <CardContent>
                <p className="text-muted-foreground">Conteúdo da página de Calendários em breve.</p>
            </CardContent>
        </Card>
    );
}
